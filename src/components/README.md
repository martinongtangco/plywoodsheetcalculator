# Components

React functional components live here. Each component should:

- Call engine functions from `src/engine/` for any calculations
- Read and write state via Zustand stores in `src/store/`
- Use Tailwind utility classes for all styling
- Never contain calculation logic (that belongs in `src/engine/`)
